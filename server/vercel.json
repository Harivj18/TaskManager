{
    "version": 1,
    "builds": [{
        "src": "./app.js",
        "use": "@vercel/node"
    }],
    "routes": [
      { 
        "source": "/(.*)", 
        "destination": "/", 
        "methods": ["GET", "POST", "PUT", "DELETE", "OPTIONS"]
     }
    ]
  }
  